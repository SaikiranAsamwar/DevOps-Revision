version: '3'

services:
  node-app:
    build: .
    container_name: node-two-tier
    ports:
      - "3000:3000"
    depends_on:
      - mongo
  
  mongo:
    image: mongo
    container_name: mongo-db
    ports:
      - "27017:27017"
